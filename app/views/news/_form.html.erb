<div class="col-md-6">
    <%= form_for(@news) do |f| %>
      <% if @news.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@news.errors.count, "error") %> prohibited this news from being saved:</h2>

          <ul>
          <% @news.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
          </ul>
        </div>
      <% end %>

      <div class="field">
        <%= f.label :image, class: "input-desc" %><br>
        <%= f.text_field :image %>
      </div>
      <div class="field">
        <%= f.label :topic, class: "input-desc" %><br>
        <%= f.text_field :topic %>
      </div>
      <div class="field">
        <%= f.label :header, class: "input-desc" %><br>
        <%= f.text_field :header %>
      </div>
      <div class="form-group">
        <%= f.label :content, class: "input-desc" %><br>
        <%= f.text_area :content, class: "form-control" %>
      </div>
      <div class="field">
        <%= f.label :link, class: "input-desc" %><br>
        <%= f.text_field :link %>
      </div>
      
        <%= f.submit class: "btn btn-success btn-border btn-sm" %>
    <% end %>
</div>